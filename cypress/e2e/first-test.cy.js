describe('BUTTON CONTAINS HI', () => {
  beforeEach('some words yes exactly and then you put an arrow func yayayayaya now where good', () => {
    cy.visit('/test')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
  it('looks inside the head content using `cy.document()`', () => {
    cy.get('#button123').contains('HI!!!!')
  })
})
